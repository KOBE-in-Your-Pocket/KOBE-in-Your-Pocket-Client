/**
 * リリース単位で機能の露出を切り替えるフラグ。
 *
 * ビルド時定数として持つ（環境変数にしない）。App Store 審査に提出したビルドと
 * 同じ挙動をチーム全員がローカルでも再現できるようにするため。
 */

/**
 * ユーザー投稿コンテンツ（レビュー投稿・編集・削除）と、その前提となるサインイン導線を
 * 有効にするか。
 *
 * v1（App Store 初回リリース）では **false**。理由:
 *
 * - **Guideline 1.2 (User-Generated Content)**: UGC を持つアプリは通報・ブロック・
 *   不適切コンテンツのフィルタ・24時間以内の対応体制が必須。v1 ではいずれも未実装。
 * - **Guideline 5.1.1(v)**: アカウント作成をアプリ内で提供する場合、アカウント削除も
 *   アプリ内で提供する必要がある。退会機能は未実装。
 *
 * 投稿とサインインを 1 つのフラグで束ねているのは、v1 におけるサインインの目的が
 * レビュー投稿だけであり、片方だけ有効にすると上記のいずれかに抵触するため。
 *
 * v1.1（モデレーション + 退会機能）で true に戻す。既存レビューの**閲覧**はこのフラグの
 * 対象外で、常に有効。
 */
export const IS_USER_CONTENT_ENABLED = false;
